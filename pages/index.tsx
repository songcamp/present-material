import type { NextPage } from 'next'
import Head from 'next/head'
import { useAccount, useContractRead } from 'wagmi'
import { Header } from '../components/Header'
import * as presentMaterialsCuratorV2 from "../contractABI/presentMaterialsCuratorV2.json"
import * as presentMaterialsCuratorV3 from "../contractABI/presentMaterialCuratorV3.json"
import EditionCard from '../components/EditionCard';
import Image from 'next/image'

const Home: NextPage = () => {

  // get account hook
  const { address, connector, isConnecting, isConnected, status} = useAccount(); 
  const currentUserAddress = address ? address : ""

  // CuratorContract Read Call --> query array of all active curators
  const { data, isError, isLoading, isSuccess, isFetching  } = useContractRead({
    addressOrName: "0x0688f06A5DF67b17D06968A7CacA51Ea5cAae569", 
    contractInterface: presentMaterialsCuratorV3.abi,
    functionName: 'viewAllCollections',
    watch: true,
    onError(error) {
        console.log("error: ", isError)
    },
    onSuccess(data) {
        console.log("Array of current collections --> ", data)
    }  
  })
  
  const collectionData = data ? data : []

  return (
    
    <div className='mb-10 flex flex-col justify-center items-center h-full min-h-screen scrollbar-thin scrollbar-thumb-black scrollbar-track-gray-600' >
      <Header />
      <Head>
        <title>Present Materials</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <div className="mt-[110px] my-4">
        <Image
          src={"/collection_page_graphic_v2.png"}
          height={227}
          width={315}
        />
      </div>
      <main className="text-white grid grid-rows-3 justify-center sm:grid-cols-3  w-full sm:w-[85%] gap-y-8 sm:gap-y-0 gap-x-0 sm:gap-x-20  h-full "> 

      {
        collectionData.map((collection, index) => {
          while (index < collectionData.length) {
            return (
              <EditionCard editionAddress={collectionData[index]} />
            )
          }
        })
      }

      </main>
    </div>
  )
}

export default Home
